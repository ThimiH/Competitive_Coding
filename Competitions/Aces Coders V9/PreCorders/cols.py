lst = list(map(int, input().split()))
N = lst[0]
K = lst[1]

order = list(map(int, input().split()))

while True:
    new = list[0]+list[1]
    list.remove(list[0])
    list.remove(list[1])
    list.append(new)

